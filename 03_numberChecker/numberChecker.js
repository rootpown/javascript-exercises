function numberChecker(number) {
  // if (number >= 10) return true;
  // else if (number >= 9 ) return false;
  // else return false;
   return  (number >= 10) ? true : (number >= 9) ? false : false;
}

// Do not edit below this line
module.exports = numberChecker;
